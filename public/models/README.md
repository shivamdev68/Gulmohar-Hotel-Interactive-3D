# Hotel model location

Place the production GLB file at:

`public/models/gulmohar.glb`

The application requests it at `/models/gulmohar.glb`. If the file is absent, the
scene continues to render normally and reports a descriptive error in the browser console.
